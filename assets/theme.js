document.documentElement.classList.remove('no-js');
document.documentElement.classList.add('js');
window.ExedraThemeCore={version:'0.2.1'};
document.addEventListener('click',function(event){const toggle=event.target.closest('[data-mobile-nav-toggle]');if(toggle){const targetId=toggle.getAttribute('aria-controls');const target=document.getElementById(targetId);if(target){const isOpen=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!isOpen));target.hidden=isOpen;}}const accordionToggle=event.target.closest('[data-accordion-toggle]');if(accordionToggle){const item=accordionToggle.closest('.accordion__item');if(!item)return;const content=item.querySelector('.accordion__content');const isOpen=accordionToggle.getAttribute('aria-expanded')==='true';accordionToggle.setAttribute('aria-expanded',String(!isOpen));if(content)content.hidden=isOpen;}const drawerOpen=event.target.closest('[data-cart-drawer-open],[data-search-drawer-open],[data-popup-open]');if(drawerOpen){const targetId=drawerOpen.getAttribute('aria-controls');const target=document.getElementById(targetId);if(target)target.classList.add('is-open');}const drawerClose=event.target.closest('[data-cart-drawer-close],[data-search-drawer-close],[data-popup-close]');if(drawerClose){const panel=drawerClose.closest('.cart-drawer, .search-drawer, .promo-popup');if(panel)panel.classList.remove('is-open');}});document.addEventListener('scroll',function(){const sticky=document.getElementById('StickyAtc');if(!sticky)return;const productMain=document.querySelector('.product-main');if(!productMain)return;const rect=productMain.getBoundingClientRect();if(rect.bottom<window.innerHeight-120)sticky.classList.add('is-visible');else sticky.classList.remove('is-visible');});(function initMotionSystem(){if(window.matchMedia&&window.matchMedia('(prefers-reduced-motion: reduce)').matches)return;const nodes=document.querySelectorAll('.motion-enabled[data-motion-trigger="viewport"]');if(!nodes.length||!('IntersectionObserver'in window)){document.querySelectorAll('.motion-enabled').forEach((node)=>node.classList.add('is-inview'));return;}const observer=new IntersectionObserver((entries)=>{entries.forEach((entry)=>{if(entry.isIntersecting){entry.target.classList.add('is-inview');observer.unobserve(entry.target);}})},{threshold:.16,rootMargin:'0px 0px -8% 0px'});nodes.forEach((node)=>observer.observe(node));document.querySelectorAll('.motion-enabled[data-motion-trigger="immediate"]').forEach((node)=>node.classList.add('is-inview'));})();